# ============================================================
# CECE-FMT — Python dependencies
# ============================================================
# Two exact versions below (shap, micom) were captured directly from pipeline run output (print(x.__version__)). All others are minimum-compatible versions for the APIs used in the notebooks —
# pin exact versions yourself with `pip freeze` once your own run is stable, for full reproducibility.

# ------------------------------------------------------------
# Core stack — Notebooks 01, 02, 03a, 03b, 05, 09, 10, 11, 12 (runs anywhere: local machine, JupyterLab or Colab)
# ------------------------------------------------------------
pandas>=2.1.0
numpy>=1.26.0
scipy>=1.11.0
scikit-learn>=1.3.0
statsmodels>=0.14.0
matplotlib>=3.8.0
seaborn>=0.13.0
networkx>=3.2
adjustText>=1.0.0
xgboost>=2.0.0
shap==0.49.1
joblib>=1.3.0

# ------------------------------------------------------------
# Notebook 06 — NOTEARS causal DAG (Google Colab) not on PyPI, installed from source
# ------------------------------------------------------------
notears @ git+https://github.com/xunzheng/notears.git

# ------------------------------------------------------------
# Notebook 08 — MICOM community metabolic modeling (Google Colab)
# ------------------------------------------------------------
micom==0.39.1
cobra>=0.29.0
optlang>=1.8.0
symengine>=0.11.0
swiglpk>=5.0.8
osqp>=0.6.5

# ============================================================
# Non-pip / system-level dependencies
# ============================================================
# FastSpar (Notebooks 04a, 04b) — compiled from source, no PyPI package:
#   git clone https://github.com/scwatts/fastspar.git
#   cd fastspar && ./autogen.sh && ./configure && make -j4
#   apt packages required: libgsl-dev, build-essential
#
# GLPK solver (backs cobra/micom LP steps in Notebook 08):
#   apt-get install glpk-utils libglpk-dev
